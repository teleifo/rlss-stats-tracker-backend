{
    "type": "object",
    "required": [
        "winner",
        "loser",
        "score",
        "season"
    ],
    "properties": {
        "winner": {
            "type": "string",
            "pattern": "^(?!\\s*$).+$"
        },
        "loser": {
            "type": "string",
            "pattern": "^(?!\\s*$).+$"
        },
        "score": {
            "type": "array",
            "items": { "type": "number" },
            "minItems": 2,
            "maxItems": 2
        },
        "season": {
            "type": "number",
            "exclusiveMinimum": 0
        }
    }
}